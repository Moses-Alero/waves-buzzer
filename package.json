{
  "name": "wavesbuzzer-chatserver",
  "version": "0.0.1",
  "description": "chat server for waves buzzer",
  "main": "index.ts",
  "scripts": {
    "dev": "nodemon --watch src --watch index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.15",
    "nodemon": "^2.0.20"
  },
  "dependencies": {
    "@prisma/client": "^4.9.0",
    "express": "^4.18.2",
    "socket.io": "^4.5.4"
  }
}
